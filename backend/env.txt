APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:l7D+GFrLim22ww+y9o8j4BL33abDySi6Y3TfscDgT2s=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Lingkungan bersama / staging / produksi: wajib MySQL (atau MariaDB) — isi DB_* sesuai server kamu.
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=arcav_hcm
DB_USERNAME=root
DB_PASSWORD=

# Opsi SQLite hanya untuk uji cepat terisolasi (tanpa server DB). Uncomment blok di bawah DAN comment mysql di atas.
# DB_CONNECTION=sqlite
# DB_DATABASE=database/database.sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Mailtrap Account Management API token (https://docs.mailtrap.io/developers/account-management/api-tokens)
# Isi token + account ID dari Mailtrap dashboard.
MAILTRAP_API_TOKEN=8024d4ba78cf74c44649f5ffd06ba341
MAILTRAP_ACCOUNT_ID=2682142
MAILTRAP_API_BASE_URL=https://mailtrap.io/api
MAILTRAP_API_TIMEOUT=10

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# HCM: alamat organisasi di header slip THR (PDF). Ganti dengan alamat kantor sebenarnya.
HCM_ORGANIZATION_ADDRESS="Gedung ${APP_NAME}, Jl. Sudirman Kav. 52-53, Jakarta Selatan 12190, Indonesia"

# Web guard: semua GET/HEAD wajib auth kecuali public_paths/public_prefixes di config/arcav_hcm_web_guard.php.
# Dev tema tanpa login: tambah sementara prefix ke public_prefixes (jangan untuk produksi tanpa review).
