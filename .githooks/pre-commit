#!/usr/bin/env bash

set -euo pipefail

# Fast checks only — does not run tests (tests run at pre-push)
bash scripts/check-api-docs-sync.sh --staged
bash scripts/lint-next-step-format.sh
bash scripts/lint-form-validation.sh
